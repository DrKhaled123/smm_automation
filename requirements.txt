# Core async runtime
asyncio-throttle==1.0.2

# Browser automation (replaces Selenium — faster, async, better stealth)
playwright==1.42.0

# HTTP clients
aiohttp==3.9.3
httpx==0.27.0
requests==2.31.0

# Steel Browser Python SDK (REST calls, no JS SDK needed)
# steel-sdk  # Uncomment if using Steel Cloud; self-hosted uses httpx directly

# Data handling
pandas==2.2.1
python-dotenv==1.0.1
pydantic==2.6.4
pydantic-settings==2.2.1

# Task scheduling
apscheduler==3.10.4

# State/caching (Redis-backed session store)
redis==5.0.3
aioredis==2.0.1

# Security / credential encryption
cryptography==42.0.5

# Retry logic
tenacity==8.2.3

# Logging
structlog==24.1.0
rich==13.7.1

# Rate limiting
limits==3.10.1

# Image handling (for media uploads)
Pillow==10.2.0

# TOTP for 2FA
pyotp==2.9.0

# Utilities
python-dateutil==2.9.0
pytz==2024.1
